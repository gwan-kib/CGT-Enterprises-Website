# CGT Enterprises Website

## Asset architecture

- Store approved logos, photographs, and other image content in `src/assets/images/`.
- Use Google Material Symbols for interface symbols; the project does not maintain a local icon directory.
- Store browser-level static files referenced by URL, such as the favicon, in `public/`.
